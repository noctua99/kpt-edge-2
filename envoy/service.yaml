apiVersion: v1
kind: Service
metadata: # kpt-merge: envoy/envoy
  name: envoy
  namespace: envoy
  annotations:
    internal.kpt.dev/upstream-identifier: '|Service|envoy|envoy'
  labels:
    testlabel2: dcn2
spec:
  selector:
    app: envoy
    testlabel2: dcn2
  ports:
  - port: 80
    targetPort: 10000
